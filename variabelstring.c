#include<stdio.h>
#include<string.h>

int main() {
    char os[6];
    strcpy(os, "Linux");  // Gunakan tanda kutip ganda yang benar
    printf("Nilai variabel os: %s\n", os);
    return 0;
}
